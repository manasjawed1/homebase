rules_version = '2';

// Hackathon MVP — open access, no auth required
// DO NOT use this in production
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
